#include "mux2.loong"

hmodule mux4(input  logic d0[3], d1[3], d2[3], d3[3],
             input  logic s[1],
             output logic y[3]) {
  logic low[3], high[3];
  mux2(d0, d1, s[0], low);
  mux2(d2, d3, s[0], high);
  mux2(low, high, s[1], y);
}
